# 个人信息台 · UI 设计方案（白色版）

**版本：** v0.5-WHITE（对应 PRD v0.5）
**对应文件：** `个人信息台-完整版-白色.html`
**设计基调：** Apple 风格 · 苹果透明玻璃（macOS Big Sur）· 黑白灰单色体系 · 明亮通透
**产品定位：** 个人工作信息收集、沉淀与长期职业记录平台（个人职业数据库 + 工作知识库 + 项目档案 + 职业成长记录）

---

## 一、设计方向

白色版走「**明亮通透的苹果玻璃**」路线：

- 页面背景为**浅色渐变**（不是纯白平铺），让玻璃背后始终有可被模糊的内容层次——这是苹果玻璃质感成立的前提；
- 玻璃卡片**高透明度 + 强模糊**，像 macOS 控制中心、访达侧栏一样"看得透、摸得到磨砂"；
- 每张玻璃卡上沿有**柔和白色高光**（光从玻璃上沿打下来）；
- 鼠标划过卡片时，玻璃内部**亮起一团纯白柔光**跟随光标；
- 背景 3 个深灰光斑缓慢漂移，提供"光线在玻璃下流动"的底层氛围。

气质关键词：**明亮、通透、冷静、克制**。避免：企业 OA、ERP 后台、密集表格、廉价科技渐变。

---

## 二、色彩体系（黑白灰单色）

### 2.1 主题变量（可直接作为开发 Tokens）

| Token | 值 | 用途 |
|---|---|---|
| `--bg` | `linear-gradient(168deg,#f6f7fa 0%,#eceff4 55%,#e4e8ef 100%)` | 页面背景（浅色渐变） |
| `--fg` | `#17181a` | 主文字（近黑） |
| `--muted` | `#6b6e76` | 次级文字（中灰） |
| `--line` | `rgba(20,22,26,.1)` | 分隔线 / 边框线 |
| `--solid-bg` | `#17181a` | 实底强调色（按钮/激活态，黑） |
| `--solid-fg` | `#f4f4f5` | 实底上的文字（白） |
| `--hover-bg` | `rgba(20,22,26,.07)` | hover 背景 |
| `--mask` | `rgba(30,32,38,.4)` | 弹窗遮罩 |

### 2.2 明度层级（黑白灰体系的核心逻辑）

| 层级 | 表达 | 场景 |
|---|---|---|
| 最高 | **黑底白字**（`--solid-bg/--solid-fg`） | 主按钮、激活 Tab、进行中状态、当前项目徽标 |
| 中 | 白玻璃卡 + 深色文字（`--fg`） | 常规卡片、正文 |
| 低 | 透明 + 中灰文字（`--muted`） | 辅助说明、未激活项、时间戳 |

**唯一彩色例外**：删除等破坏性操作使用功能红（如 `#D70015`），只表达危险语义，不参与品牌表达。

---

## 三、苹果透明玻璃配方（核心）

### 3.1 玻璃面参数

| 参数 | 值 |
|---|---|
| 玻璃填充 | `linear-gradient(160deg, rgba(255,255,255,.70), rgba(255,255,255,.46))` |
| 背景模糊 | `blur(42px) saturate(1.9)` |
| 边框 | `1px solid rgba(255,255,255,.62)` |
| 圆角 | `22px` |
| 阴影 | `0 30px 70px rgba(30,32,38,.10), inset 0 1px 0 rgba(255,255,255,.95), inset 0 -1px 0 rgba(30,32,38,.04)` |

### 3.2 三层结构（每张玻璃卡）

```css
/* 第一层：玻璃本体（背景 + 模糊） */
.glass {
  background: var(--glass-bg);
  backdrop-filter: var(--glass-blur);
  border: 1px solid var(--glass-border);
  border-radius: 22px;
  box-shadow: var(--shadow);
}

/* 第二层：顶部高光（Apple signature） */
.glass::before {
  background: linear-gradient(180deg,
    rgba(255,255,255,.42), rgba(255,255,255,.08) 34%, transparent 58%);
  mix-blend-mode: overlay;
}

/* 第三层：光标追光（纯白柔光） */
.glass::after {
  background: radial-gradient(280px 180px at var(--mx,30%) var(--my,20%),
    rgba(255,255,255,.6), transparent 62%);
  mix-blend-mode: overlay;
  opacity: 0;  /* hover 时 1 */
}
```

内容层统一 `.glass > * { position: relative; z-index: 1 }`，保证文字始终在光效之上清晰可读。

### 3.3 动态玻璃（三要素）

| 要素 | 参数 | 说明 |
|---|---|---|
| **背景光斑** | 3 个深灰圆（`#9ba1ac / #828896 / #a8adb8`），`blur(80px)`，透明度 `.55`，20s 漂移（位移 ±110px + 缩放 1.16/0.9） | 背景有层次 → 玻璃模糊才有"磨砂折射" |
| **卡片追光** | 纯白径向光 `.55/.6` + `overlay` 混合，280×180px 跟随鼠标（`--mx/--my`），hover 淡入 400ms | 玻璃内部"亮起"而非变暗 |
| **hover 上浮** | `translateY(-6px)` + 阴影加强 `0 32px 70px rgba(30,32,38,.14)` | 细腻点击反馈 |

> 设计取舍：背景光斑用**深灰**（纯白光斑在浅底上会隐形）；卡片光效用**纯白**（白玻璃上呈提亮）。两层分工，动态都可见且克制。

---

## 四、字体与排版

### 4.1 字体栈

```
font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
             "PingFang SC", "Helvetica Neue", "Microsoft YaHei", sans-serif;
```

### 4.2 字号阶梯（正文基础已整体 +2px 提升可读性）

| 用途 | 字号 | 字重 |
|---|---|---|
| 首页大日期 | `clamp(54px, 7vw, 94px)` | 200 + 数字段 700 |
| 页面大标题 | 30px | 700 |
| 卡片标题 | 19px | 600 |
| 正文 | 15.5–16px | 400 |
| 次级说明 | 14px | 400 |
| 标签 / 时间戳 | 12.5–13px | 500 |
| 迷你徽标 | 11.5px | 700 |

数字一律 `font-variant-numeric: tabular-nums`（对齐、不跳动）。

### 4.3 间距 / 圆角 / 动效

| Token | 值 |
|---|---|
| 栅格 | 4pt 基准，间距 8/12/16/24/32 |
| 圆角 | 10（按钮）/ 14（输入）/ 22（卡片）/ 999（胶囊） |
| 动效 | `cubic-bezier(.2, 0, 0, 1)`，即时 120ms / 常规 200ms / 慢 320ms |
| 光斑循环 | 20s |

---

## 五、布局结构

### 5.1 顶部双层导航（固定悬浮）

```
┌────────────────────────────────────────────────┐
│ ⬡ WORKSPACE · PERSONAL OS   [搜索… ⌘K]          │ ← 第一行：品牌 + 窄搜索（340px）+ 留白
├────────────────────────────────────────────────┤
│ 首页 当前项目 项目库 客户信息库 个人成就          │ ← 第二行：目录（自适应宽度，无尾部留白）
│ 个人知识库 灵感记录 · 时间轴 附件中心            │
└────────────────────────────────────────────────┘
```

- 顶栏 `width: max-content; max-width: 96vw`，宽度贴合目录行，右侧无空荡留白；
- 第一行 `flex-wrap: nowrap`，搜索框固定 `min(340px, 42vw)` 不再拉伸；
- 目录项：纯文字行（无胶囊底色），激活项 `font-weight:700` + **底部 18px 短横线**指示；
- 分隔符：4px 圆点（「灵感记录」与「时间轴」之间）；
- 主内容 `padding-top: 200px`（桌面）/ 210px（移动），与顶栏保持呼吸间距。

### 5.2 页面内容区

`main` 最大宽 1240px 居中，`padding: 200px 44px 70px`。所有内容以玻璃卡承载，卡片 → 详情两级浏览。

---

## 六、页面规范（9 页）

### 6.1 首页 `/`

```
8月15日  星期六 · 上海        ← hero-line 水平排版（基线对齐）
[当前待办]   [最近时间轴]     ← 双栏玻璃卡
12 事项 | 8 联系人 | 23 知识 | 5 灵感  ← 统计条
```

- 大日期与"星期 · 地点"**同一行**（`align-items: baseline`，gap 32px）；
- 当前待办：只读当前项目未完成事项，勾选实时联动；点击跳转当前项目；
- 最近时间轴：按日分组，>6 条折叠（展开更多）；
- 不做复杂仪表盘。

### 6.2 当前项目 `/project/current`（二级导航 6 子页）

项目概览｜工作事项｜周期工作｜工作流程｜SOP｜客户信息

- **项目概览**：默认字段（名称/体量/性质/人数/地址/联系电话/甲方/开始-结束时间/服务模式/岗位/职责/简介/备注）+「＋ 添加自定义字段」；左上角「＋ 新增」统一入口（选择内容类型 → 自动归入对应目录）；
- **工作事项**：分类筛选胶囊（公司对接/甲方对接/个人工作/紧急处理/项目待办）+ 两态标签（待处理=灰、已完成=白底灰描边）+「＋ 新增事项」；标题选填、内容必填、**无截止时间**；>6 条折叠；删除二次确认；
- **周期工作**：每日/每周/每月/每季度/每半年/每年 分组 +「＋ 新增工作」；
- **工作流程**：流程卡片库 → 点击进入 GF-01 玻璃胶囊流详情（浏览模式只显示已建节点；编辑模式才出现控件）+「＋ 新增流程」；
- **SOP**：SOP 卡 + 附件列表（来源链） +「＋ 新增 SOP」；
- **客户信息**：关系分层卡（姓名/职位/公司/关联项目标签/工作关系摘要）+「＋ 新增客户」（自动关联当前项目，可追加）。

### 6.3 项目库 `/projects`

当前项目 + 历史项目卡片（名称/性质/时间/岗位/当前·已结束状态）。结束项目 = 填结束时间 → 进项目库，数据完整保留。「＋ 新建项目」。

### 6.4 客户信息库 `/clients`

全部客户总库：项目筛选 + 姓名搜索；客户卡必须体现关联项目；多项目关联（筛选 A 或 B 都能出现）。卡片 → 详情（动态字段，只显示已填写）。「＋ 新增客户」。

### 6.5 个人成就 `/achievements`

成就卡片库 → 详情页。卡：名称/时间/相关主题/相关项目；详情：+ 说明（选填）+ 附件。「＋ 新增成就」。

### 6.6 个人知识库 `/knowledge`（三级结构）

知识主题卡 → 主题详情 → 知识详情。主题页「＋ 新增主题」；主题内「＋ 新增知识」（自动关联当前主题）；先记录再整理，无 AI。

### 6.7 灵感记录 `/ideas`

轻量小卡片流：内容直接展示，长内容折叠；分类/标签/时间筛选 + 关键词搜索。「＋ 新增灵感」；不增加转换功能。

### 6.8 时间轴 `/timeline`

年 → 月 → 日 完整回顾；按时间/项目/内容类型筛选；点击直达内容。

### 6.9 附件中心 `/attachments`

附件列表 + 来源链（来源内容 + 所属项目），点击跳回原内容。

### 6.10 全局搜索（⌘K）

顶栏入口 + ⌘K 唤起；结果按类型分组（客户/事项/知识/灵感…）；输入实时过滤；点击直达。

---

## 七、组件规范

| 组件 | 规格 |
|---|---|
| 主按钮 | 黑底白字（`--solid-bg`），圆角 10px，高 38px，hover 深灰 |
| 次要按钮 | 白玻璃 + 灰描边，hover 边框加深 |
| 危险按钮 | 红色系（功能语义） |
| 标签 | 胶囊形，浅灰底 / 黑底白字（当前项目徽标） |
| 状态 | 待处理=浅灰底中灰字；已完成=白底灰描边 |
| 输入框 | 白玻璃 `rgba(255,255,255,.7)` + 灰描边，聚焦描边加深 |
| 卡片 | 玻璃三层结构（见 §3.2），hover 上浮 6px |
| 弹窗 | `--mask` 遮罩 + 玻璃弹窗，280ms 弹入 |
| 展开更多 | 列表 >6 条出现，展开/收起 |

---

## 八、交互与全局规则（PRD v0.5 锁定项落地）

1. 同一时间仅 1 个当前项目（项目库「当前」徽标唯一）；
2. 每模块独立新增入口（＋ 新增事项/工作/流程/SOP/客户/成就/主题/知识/灵感/项目）；
3. 当前项目内新增 → 自动关联当前项目（新增客户自动带入当前项目，可追加）；
4. 删除重要信息 → 二次确认弹窗（防误触）；
5. 列表 >6 条默认折叠（展开更多/收起）；
6. 详情页只显示已填写字段（无"暂无/未填写/-"）；
7. 客户/项目支持自定义字段（填写才展示）；
8. 工作事项：分类必选、标题选填、内容必填、无截止时间、两态（待处理/已完成）；
9. 工作流程统一 Glass Flow · GF-01（浏览/编辑双模式）；
10. 卡片 → 点击 → 详情（客户/项目/流程/成就/知识主题/单条知识/灵感）；
11. 无 AI 自动整理；暂不增加数据导出备份。

---

## 九、无障碍与性能

- 全部动效尊重 `prefers-reduced-motion`（动画/光斑/追光关闭）；
- 图标按钮带 `aria-label`；装饰图标 `aria-hidden`；
- 焦点可见（`focus-visible` 描边）；
- 数字列 `tabular-nums`；
- 玻璃用 `backdrop-filter` + 前缀兼容；光斑数量克制（3 个），避免动画卡顿；
- 颜色对比：正文 `#17181a` on 白玻璃，对比度充足。

---

## 十、技术落地建议

- 用 CSS 变量承载全部 Tokens（`--bg/--fg/--glass-bg/--glass-blur/--shadow` 等），主题切换只改变量；
- 玻璃三层结构（本体/顶部高光/追光）拆成 `.glass` + `::before` + `::after`，组件级复用；
- 目录与卡片 → 详情建议拆为独立路由（当前实例为 SPA 演示）；
- 可平滑演进：换色 = 改 `:root` 变量；换动效强度 = 改 `--glass-blur` 与光斑透明度。

*本方案与 `个人信息台-完整版-白色.html` 一一对应，所有参数可直接作为开发规范。*
